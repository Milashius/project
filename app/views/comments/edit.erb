<h1>Editting Comment</h1>

<%= form_for(@comment, url: idea_comment_path(@idea, @comment), method: :put) do |f| %>
<%= render 'form', comment: @comment, f: f %>
<%end %>

<%= link_to 'Back', idea_comments_path(@idea) %>

